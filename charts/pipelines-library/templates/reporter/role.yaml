{{- if eq .Values.reporter.enabled true }}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "edp-tekton-reporter.name" . }}
  labels:
    {{- include "edp-tekton-reporter.labels" . | nindent 4 }}
rules:
  - verbs:
      - get
      - list
      - watch
      - patch
    apiGroups:
      - tekton.dev
    resources:
      - pipelineruns

  # TaskRuns are fetched one by one from the PipelineRun's childReferences via
  # an uncached reader; the reporter never lists or watches them.
  - verbs:
      - get
    apiGroups:
      - tekton.dev
    resources:
      - taskruns

{{- if .Values.reporter.logsReporting }}
  - verbs:
      - get
    apiGroups:
      - ''
    resources:
      - pods/log
{{- end }}

  # Dropped rather than emitted empty: an empty resourceNames grants every
  # secret in the namespace, not none.
{{- $reporterSecrets := include "edp-tekton-reporter.secretNames" . | fromJsonArray }}
{{- if $reporterSecrets }}
  - verbs:
      - get
    apiGroups:
      - ''
    resources:
      - secrets
    resourceNames:
      {{- range $reporterSecrets }}
      - {{ . | quote }}
      {{- end }}
{{- end }}

  # The git server (and its owning codebase) is resolved by name through an
  # uncached reader, so only get is needed; plain reads return status inline
  # without subresource grants.
  - verbs:
      - get
    apiGroups:
      - v2.edp.epam.com
    resources:
      - codebases
      - gitservers

  # Leader election.
  - verbs:
      - get
      - list
      - watch
      - create
      - update
      - patch
      - delete
    apiGroups:
      - coordination.k8s.io
    resources:
      - leases

  - verbs:
      - create
      - patch
    apiGroups:
      - ''
    resources:
      - events
{{- end }}
