apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "tekton-reporter.serviceAccountName" . }}
  labels:
    {{- include "tekton-reporter.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
